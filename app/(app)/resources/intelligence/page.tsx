import { BookOpen, Sparkles } from "lucide-react";
import { PageHeader } from "@/components/shell/page-header";
export default function ResourceIntelligencePage() { return <div className="flex flex-col gap-6"><PageHeader eyebrow="Resources / Intelligence" title="Resources for your weak areas" description="Recommendations are clearly separated from resources you have saved." /><div className="card border-brand-200 bg-brand-50"><div className="flex items-start gap-3"><Sparkles className="mt-0.5 size-4 text-brand-600" /><div><p className="font-semibold">You marked Normalization as a weak area.</p><p className="mt-1 text-sm text-muted">Three saved resources may help. Start with the verified notes below.</p></div></div></div><div className="grid gap-4 md:grid-cols-3">{["Database normalization notes", "Practice: relational algebra", "DBMS assessment guide"].map((title, i) => <div className="card" key={title}><div className="flex items-center justify-between"><BookOpen className="size-5 text-brand-600" /><span className="eyebrow">{i === 0 ? "Saved" : "Recommended"}</span></div><h2 className="mt-6 font-semibold">{title}</h2><p className="mt-2 text-xs leading-relaxed text-muted">Database Management Systems · Verified by your workspace</p></div>)}</div></div>; }
