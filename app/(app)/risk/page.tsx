import { AlertTriangle, ArrowRight } from "lucide-react";
import { PageHeader } from "@/components/shell/page-header";
import { Button } from "@/components/ui/button";
export default function RiskPage() { return <div className="flex flex-col gap-6"><PageHeader eyebrow="Intelligence" title="Academic risk" description="Evidence-based signals from your workload, never fabricated predictions." /><div className="card border-warning/30"><div className="flex items-start gap-4"><span className="rounded-lg bg-warning/15 p-3 text-warning"><AlertTriangle className="size-5" /></span><div><p className="eyebrow">Workload risk</p><h2 className="mt-2 text-3xl font-semibold">Elevated</h2><p className="mt-2 max-w-xl text-sm leading-relaxed text-muted">Three deadlines within five days, six incomplete tasks, and two subjects below your recent completion rate.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-muted/40 p-4"><p className="text-2xl font-semibold">3</p><p className="mt-1 text-xs text-muted">deadlines in 5 days</p></div><div className="rounded-lg bg-muted/40 p-4"><p className="text-2xl font-semibold">6</p><p className="mt-1 text-xs text-muted">incomplete tasks</p></div><div className="rounded-lg bg-muted/40 p-4"><p className="text-2xl font-semibold">4.5h</p><p className="mt-1 text-xs text-muted">planned study time</p></div></div><Button className="mt-6">Review schedule <ArrowRight data-icon="inline-end" /></Button></div></div>; }
